#include <iostream>
#include <cmath>

int main() {
    // Оголошення змінних
    double A = 30.0;  // Кут в градусах
    double Y;         // Результат

    // Асемблерна вставка
    __asm {
        fld1                   // Завантаження 1 у стек
        fldpi                  // Завантаження числа Pi у стек
        fld A                  // Завантаження кута A (30 градусів)
        fld A                  // Завантаження кута A знову для обчислення косинуса
        fdiv st(0), st(1)      // Розподіл кута на 180 (перетворення градусів у радіани)
        fsin                   // Обчислення sin(A)
        fld A                  // Знову завантажуємо кут для cos(A)
        fdiv st(0), st(1)      // Перетворення у радіани
        fcos                   // Обчислення cos(A)
        fmul                   // Множення sin(A) * cos(A)
        fld1                   // Завантаження 1 у стек
        fadd                   // Додавання 1 до результату
        fldl2e                 // Завантаження логарифму 2
        fmul                   // Множення результату на 2
        fpatan                 // Обчислення арктангенса
        fld1                   // Завантаження 1 у стек для множення
        fadd                   // Множення на 2
        fstp Y                 // Збереження результату в змінну Y
    }

    // Виведення результату
    std::cout << "Y = " << Y << std::endl;

    return 0;
}
